# Snake Replica

A self-contained classic Snake game built with plain HTML, CSS, and JavaScript.

## Controls
- Arrow keys or WASD: move
- Space: pause/resume
- Enter: restart after game over

Open `index.html` in a browser. No dependencies or build step required.
